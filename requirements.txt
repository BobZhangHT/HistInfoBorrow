# Requirements for CAHB-PP Simulation Study
# ==========================================
#
# Core computational libraries for adaptive randomization simulation,
# statistical analysis, and publication-quality visualization.
#
# Installation:
#   pip install -r requirements.txt
#
# Or with conda:
#   conda create -n cahb-pp python=3.9
#   conda activate cahb-pp
#   pip install -r requirements.txt

# Core Scientific Computing
# --------------------------
numpy>=1.21.0,<2.0.0          # Numerical computing and array operations
scipy>=1.7.0,<2.0.0           # Statistical distributions and special functions

# Data Manipulation and Analysis
# --------------------------------
pandas>=1.3.0,<2.0.0          # DataFrame operations for results aggregation

# Visualization
# -------------
matplotlib>=3.4.0,<4.0.0      # Publication-quality plotting (PDF outputs)
seaborn>=0.11.0,<1.0.0        # Statistical visualization and color palettes

# Parallel Computing and Caching
# --------------------------------
joblib>=1.0.0,<2.0.0          # Parallel execution and checkpointing
tqdm>=4.62.0,<5.0.0           # Progress bars for monitoring simulations
numba>=0.56.0                 # Accelerate CAHB-PP sampling kernels

# Optional: Performance and Development
# ---------------------------------------
# Uncomment if needed for faster computation or development

# numba>=0.54.0                # JIT compilation for numerical functions
# psutil>=5.8.0                # Memory monitoring and management
# black>=21.0                  # Code formatting (development)
# flake8>=3.9.0                # Linting (development)
# pytest>=6.2.0                # Testing framework (development)

# System Requirements
# -------------------
# Python: >= 3.8, < 3.11 (tested on 3.9)
# OS: Windows, macOS, Linux
# RAM: >= 8 GB recommended (for parallel simulations)
# Disk: >= 2 GB for cache and results

# Version Compatibility Notes
# ----------------------------
# - numpy <2.0 for stability (some methods may not work with numpy 2.x yet)
# - pandas <2.0 for compatibility with analysis code
# - matplotlib/seaborn versions tested for PDF output quality
# - joblib for persistent caching and memory-efficient parallelization
# - All versions tested on Python 3.9 (recommended)

# Installation Verification
# --------------------------
# After installation, verify with:
#   python -c "import numpy, scipy, pandas, matplotlib, joblib, tqdm; print('All packages installed successfully')"

